services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: rotary_app

    ports:
      - "8000:8000"

    env_file:
      - .env

    environment:
      PORT: 8000
      RUN_MIGRATIONS: "true"
